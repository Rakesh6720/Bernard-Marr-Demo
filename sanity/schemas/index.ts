import article from "./article-schema";
import project from "./project-schema";
import video from "./video-schema";
import service from "./service-schema";

const schemas = [project, article, video, service];

export default schemas;
